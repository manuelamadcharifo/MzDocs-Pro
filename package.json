{
  "name": "mzdocs-pro",
  "version": "4.0.0",
  "description": "MzDocs Pro v4 — Documentos com IA para Moçambique",
  "scripts": {
    "build": "node build.js",
    "dev": "npx http-server dist -p 3000 --cors",
    "test": "node --check assets/js/app.js && node --check netlify/functions/generate-document.js && node --check netlify/functions/process-payment.js && echo 'Sintaxe OK'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
